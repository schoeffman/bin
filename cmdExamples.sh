echo "This files exists to give examples to common commands that I always forget"


#find a line with apach2 in it under and folder that starts with rc
#execute a command to remove that line and replace it with nothing
#sudo find ./rc* -name \*apache2 -exec rm {} \;
